USE [SIOPEL_INTERFACE_DB]
GO

 

CREATE  PROCEDURE [dbo].[P_GEN_CEVALDOM_OPER_CODIGO] 
AS
SELECT TOP 1
	ABS(CHECKSUM(NEWID())) orden
   ,*
FROM CodigoAccesoCevaldom
ORDER BY orden DESC
GO


